# Keep Cursor's AI codebase indexer out of build output and dependencies.
# These are generated/vendored — indexing them wastes CPU, memory, and index budget.
.next/
node_modules/
public/studio/
dist/
out/
.git/

# Generated Sanity artifacts (regenerated by `npm run typegen`)
sanity.types.ts
schema.json
